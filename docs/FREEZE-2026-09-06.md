# aekr-web — informe de freeze y cierre de fase

Fecha de corte: 2026-09-06. Autoridad: Ed, HOC.
Estado de fase: **CERRADA / FROZEN**, por aceptación explícita del propietario.
Estado del informe: **DONE_WITH_CONCERNS**; las limitaciones se conservan abajo.

## Decisión y alcance

Instrucción de Ed: «Aekr web está bien y entra en freeze ahora. Documenta el
freeze con un estado total del repo en un md informe, y damos por cerrada esta
fase. Cualquier cosa que quede pendiente documéntalo en el informe que te dije
como proxima fase. Procede en full.»

Se congela la fase de profesionalización, experiencia visual, navegación y
EN/SP del sitio público. No hay nuevas mejoras funcionales en curso. La
autenticación consultada pertenece a accreatio y no es un pendiente de esta web.
kommiBo es otro proyecto con Worker propio en `https://aekr.io/kommibo/`; su autenticación,
consola, credenciales, ejecución y pendientes se gobiernan en su repositorio.

El freeze es una decisión documental del propietario, no un bloqueo técnico de
GitHub o Cloudflare. No cambia switches, permisos, infraestructura ni schedulers.
La integración existente de Workers Builds puede desplegar tras un push.
Este corte no ejecuta commit, push, PR, merge ni deployment: preserva la versión
pública aceptada y los cambios locales, y entrega el informe solicitado en disco.
Una nueva instrucción explícita del propietario debe reabrir el trabajo.

## Estado Git y entrega

| Elemento | Observación del corte |
|---|---|
| Repositorio | checkout local de `aekr-web` |
| Remoto | `https://github.com/edlopezpm-ops/aekr-web.git` |
| Rama | `master` |
| HEAD y master remoto consultado | `ec2128639bb85384467d77d40202e940e2a94dcc` |
| Release | PR #13: restore bitmap motion and simplify language switching |
| Checks GitHub del commit | `validate` y `Workers Builds: aekr-web`: completed/success |
| PR abiertos al consultar | Ninguno |
| Inventario inicial versionado | 43 archivos; 13 en `public/` |
| Cambios heredados sin commit | `README.md`, `docs/master.md`, `tests/sections.mjs`: +61/-3 antes de este informe |
| Cambio de este corte | Este informe nuevo y estado/enlace de freeze en `docs/master.md` |
| Estado final de entrega | Release público aceptado; documentación y fixture locales sin entregar a Git |

Los cambios heredados corrigen el inventario de scripts del README, documentan
el cierre y hacen que el test espere la decodificación del banner antes de medir
la posición del hero. Se preservan íntegros. El freeze no significa checkout
limpio ni que estos cambios estén en el remoto. No hay cambios de producción
pendientes en `public/`, `src/index.js` o `wrangler.jsonc` al corte.

## Estado funcional y arquitectura

| Área | Estado |
|---|---|
| Web pública | HTML/CSS/JS estáticos; sin framework, bundler ni dependencias npm de runtime |
| Contenido y navegación | Siete vistas; navegación por teclado, touch e historial |
| Idiomas | Selector EN/SP local |
| Movimiento | Bitmap, atmósfera y transiciones; fallback de movimiento reducido; aceptado por Ed |
| Contacto | Worker POST a `https://aekr.io/api/contact`, validación, honeypot, throttle en memoria y email server-side |
| Autenticación propia del sitio | No implementada ni requerida para la web pública |
| Hosting | Cloudflare Worker `aekr-web`, static assets limitados a `public/` |
| Gobernanza | Lean / Mode 0; autoridad humana final; sin runtime AEKR local |
| Documentación | README, `docs/master.md`, Project Map HTML/Markdown/JSON y registros de verificación/rollback |
| Herramientas | Checks locales, suite de navegador y CI de validación; no APIs LLM |

## Verificación de este corte

- `npm run check`: PASS, incluidos integridad de 13 archivos públicos, lifecycle
  de atmósfera, gobernanza/provenance, mapa y 10 casos del validador de URLs.
- Suite completa de navegador: PASS (exit 0), navegación, móvil, EN/SP,
  formularios interceptados, texto ampliado, movimiento reducido, fallback sin
  JavaScript y cero errores de página. No equivale a recepción real de email.
  La primera invocación sin configuración falló por ausencia del paquete local
  Playwright. Se reutilizó la instalación existente mediante `PLAYWRIGHT_MODULE`
  y Brave mediante `BROWSER_EXECUTABLE`, sin instalar dependencias.
- HTTP con curl: `https://aekr.io/`, `styles.css`, `sections.js`, `i18n.js` y `script.js`
  respondieron 200 con bytes idénticos al checkout. URL desconocida: 404.
- `https://aekr.io/404.html` redirige 307 a `https://aekr.io/404`; no tratar la respuesta inicial como contenido
  divergente del archivo: siguiendo la redirección, los bytes coinciden. `robots.txt` incorpora un bloque identificado como
  Cloudflare Managed Content además del contenido local: no hay identidad total
  de bytes para ese recurso.
- La consulta inicial con urllib recibió 403 en múltiples recursos; curl permitió
  verificar los anteriores. Esto limita la afirmación de accesibilidad universal;
  no demuestra por sí solo una caída de la web ni un defecto de código.
- `git diff --check`: PASS. Tras redactar el informe, el validador rechazó
  referencias absolutas ambiguas; se usaron URLs completas y referencias
  portables, y `npm run check` volvió a PASS sin cambiar el validador.
- No se envió email, no se probaron credenciales y no se cambiaron servicios remotos.
- Revisión documental en el mismo contexto; no es Audit independiente ni Full Audit.
  El cierre de fase lo autoriza Ed; la evidencia técnica no sustituye su autoridad.

## Próxima fase — backlog, no iniciada

| Prioridad | Pendiente | Condición de cierre futura |
|---|---|---|
| Media | Entregar documentación y fixture locales a Git | Revisar diff, validar y entregar con alcance explícito considerando el despliegue automático por push |
| Media | Recepción real del formulario | Envío controlado autorizado y confirmación de recepción en inbox; mocks o HTTP 200 no bastan |
| Media | Controles efectivos de deployment | Inspeccionar configuración vigente de builds, rama, permisos y orden de validación/despliegue |
| Media | Facturación y entitlement de Cloudflare | Verificar plan, consumo y condiciones actuales; no inferir costo cero |
| Media | Protección durable contra abuso del formulario | Evaluar necesidad y control adecuado; el throttle actual en memoria es best effort |
| Baja | Política HTTP/crawlers | Confirmar intención del robots administrado y explicar diferencias de acceso por cliente si son relevantes |
| Baja | Recuperación actual | Revalidar backups y versión exacta del Worker antes de un rollback; los IDs documentados son históricos |

No se incluyen autenticación de accreatio ni mejoras del executor de kommiBo en
este backlog. No se declara ninguna de estas tareas iniciada o autorizada para
ejecución automática por el hecho de listarla.

## Recuperación y mínimo cambio suficiente

El commit de referencia del freeze es el HEAD completo indicado arriba. Existen
procedimientos y referencias históricas en `docs/rollback.md` y `docs/master.md`;
no se restauraron ni revalidaron bundles en este corte. Conservar el diff local
antes de cualquier operación de recuperación; no resetear, limpiar ni sobrescribir
los tres archivos heredados. Un rollback de Cloudflare no revierte Git y viceversa.

Se reutiliza la fuente de verdad existente y se añade únicamente el informe
Markdown pedido por Ed. Se inspeccionaron estado/diff, README, master, mapa,
rollback, scripts de validación y checks del release. No era necesario introducir
dependencias, abstracciones, interfaces, configuración, componentes de mapa ni
código productivo. Complejidad de runtime: delta cero. Los registros anteriores
permanecen como evidencia fechada; el encabezado de freeze gobierna el estado actual.

Inventario final: README +4/-2; master +57/-1; test de navegador +6/-0;
informe nuevo. Sólo master y este informe se editaron en esta sesión.
